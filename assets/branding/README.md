# Branding assets

The selected app icon is stored in `appicon/`.

- `gym-assistant-icon-master.png` is the original generated source (1,254×1,254).
- The numbered PNGs are practical raster exports for UI and documentation use.
- Keep the master unchanged; create new versioned exports if the artwork is revised.

`github-social-preview.png` is a wide composition with the icon and repository name, ready to upload in GitHub repository Settings → Social preview. GitHub recommends a PNG, JPG, or GIF under 1 MB and at least 640×320 (1,280×640 is recommended); this export is 1,774×887 and under 1 MB.
